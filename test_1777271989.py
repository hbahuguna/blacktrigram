{
  "file_path": "tests/unit/systems/physics/test_collision_detection.py",
  "code": "import pytest\n\n# --- Mocks and Test Doubles ---\n# As the target code is unavailable, we are creating Python representations\n# of the data structures and the target function based on its summary.\n\nANATOMICAL_CONSTANTS = {\n    \"head\": {\"x\": 0, \"y\": 1.6, \"width\": 0.3, \"height\": 0.4},\n    \"torso\": {\"x\": -0.2, \"y\": 0.8, \"width\": 0.4, \"height\": 0.8},\n    \"left_arm\": {\"x\": -0.5, \"y\": 0.8, \"width\": 0.3, \"height\": 0.7},\n    \"right_arm\": {\"x\": 0.2, \"y\": 0.8, \"width\": 0.3, \"height\": 0.7},\n    \"legs\": {\"x\": -0.15, \"y\": 0, \"width\": 0.3, \"height\": 0.8},\n}\n\nclass BoundingBox:\n    \"\"\"A mock representation of a spatial bounding box.\"\"\"\n    def __init__(self, x=0, y=0, width=0, height=0):\n        self.x = x\n        self.y = y\n        self.width = width\n        self.height = height\n\n    def __eq__(self, other):\n        return (\n            isinstance(other, BoundingBox) and\n            self.x == other.x and\n            self.y == other.y and\n            self.width == other.width and\n            self.height == other.height\n        )\n\n    def __repr__(self):\n        return f\"BoundingBox(x={self.x}, y={self.y}, width={self.width}, height={self.height})\"\n\nclass BodyPart:\n    \"\"\"A mock representation of an anatomical region with a bounding box.\"\"\"\n    def __init__(self):\n        self.bounding_box = BoundingBox()\n\nclass Character:\n    \"\"\"A mock representation of the character model.\"\"\"\n    def __init__(self):\n        self.head = BodyPart()\n        self.torso = BodyPart()\n        self.left_arm = BodyPart()\n        self.right_arm = BodyPart()\n        self.legs = BodyPart()\n\n\ndef initialize