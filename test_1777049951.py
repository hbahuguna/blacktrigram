{
  "file_path": "tests/unit/systems/physics/test_collision_detection.py",
  "code": "import pytest\nfrom unittest.mock import Mock\nimport math\n\n# In a real-world scenario, this SUT (System Under Test) would be imported.\n# As the original TypeScript code is unavailable, we provide a Python implementation\n# that matches the described logic for creating a valid, standalone test module.\n\ndef checkAttackHit(world, attacker_id, defender_id, technique, raycast_fn):\n    \"\"\"\n    Evaluates whether a combat technique successfully connects with a defender.\n\n    Args:\n        world (dict): A dictionary representing the game state, mapping entity IDs to components.\n        attacker_id (str): The ID of the attacking entity.\n        defender_id (str): The ID of the defending entity.\n        technique (dict): A dictionary describing the attack, e.g., {'range': 5.0}.\n        raycast_fn (callable): A function that performs a raycast and returns the hit result.\n\n    Returns:\n        bool: True if the attack hits, False otherwise.\n    \"\"\"\n    # Helper to safely get components from the world