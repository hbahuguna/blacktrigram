{
  "file_path": "tests/unit/combat/test_combat_input.py",
  "code": "import pytest\n\n# --- Mock handleCombatInput function ---\n# This mock simulates the behavior of the handleCombatInput function\n# based on its summary: \"Orchestrates player combat actions and menu toggles\n# by processing keyboard input events relative to the current game state.\"\n# In a real scenario, this would either be a Python binding to the original\n# TypeScript function or a direct import if the target function were in Python.\n\ndef handle_combat_input_mock(event: dict, game_state: dict) -> dict:\n    \"\"\"\n    Simulates the processing of a keyboard input event in a combat screen.\n    \n    Args:\n        event (dict): A dictionary representing the keyboard event, e.g.,\n                      {'key': 'z', 'type': 'keydown'}.\n        game_state (dict): A dictionary representing the current game state,\n                           e.g., {'mode': 'combat', 'is_paused': False, 'player_status': 'normal'}.\n\n    Returns:\n        dict: A dictionary indicating the action taken and any state changes.\n              e.g., {'action': 'attack', 'state_change': {}}.\n    \"\"\"\n    key = event.get('key')\n    event_type = event.get('type')\n    \n    # Only process keydown events for actions in this mock\n    if event_type != 'keydown':\n        return {\"action\": \"none\", \"state_change\": {}}\n\n    current_mode = game_state.get('mode', 'combat')\n    is_paused = game_state.get('is_paused', False)\n    player_status = game_state.get('player_status', 'normal')\n    inventory_open = game_state.get('inventory_open', False)\n\n    # Handle menu toggles first, as they often override combat actions\n    if key == 'escape':\n        if current_mode == 'combat':\n            new_paused_state = not is_paused\n            return {\"action\": \"toggle_pause_menu\", \"state_change\": {\"is_paused\": new_paused_state}}\n        # Add logic for escape in other modes if applicable\n        return {\"action\": \"none\", \"state_change\": {}}\n    \n    if key == 'tab':\n        if not is_paused: # Inventory might be accessible only when not paused\n            return {\"action\": \"toggle_inventory\", \"state_change\": {\"inventory_open\": not inventory_open}}\n        return {\"action\": \"none\", \"state_change\": {}}\n\n    # If the game is paused, most combat actions should be ignored\n    if is_paused:\n        return {\"action\": \"none\", \"state_change\": {}}\n\n    # If player is in a status that prevents actions (e.g., stunned)\n    if player_status == 'stunned':\n        return {\"action\": \"none\", \"state_change\": {}}\n\n    # Handle combat actions\n    if key == 'z':  # Example: Attack\n        return {\"action\": \"attack\", \"state_change\": {}}\n    elif key == 'x':  # Example: Block\n        return {\"action\": \"block\", \"state_change\": {}}\n    elif key == 'c':  # Example: Dodge\n        return {\"action\": \"dodge\", \"state_change\": {}}\n    \n    # Default: no recognized action\n    return {\"action\": \"none\", \"state