{
  "file_path": "tests/unit/components/screens/combat/test_combat_screen_3d_keydown.py",
  "code": "import pytest\n\n# --- Simulated Target Code (Python equivalent of handleKeyDown logic) ---\n# Since the original target code is in TypeScript and unavailable, this\n# Python class and function simulate the expected behavior of handleKeyDown\n# based on its summary: \"Manages combat-specific keyboard shortcuts for\n# toggling UI overlays and performance monitoring tools.\"\n\nclass MockCombatScreenState:\n    \"\"\"\n    A mock object to simulate the state managed by CombatScreen3D.\n    \"\"\"\n    def __init__(self):\n        self.ui_overlay_visible = False\n        self.performance_monitor_visible = False\n        self.last_action = None # To track which toggle method was called\n\n    def toggle_ui_overlay(self):\n        self.ui_overlay_visible = not self.ui_overlay_visible\n        self.last_action = \"toggle_ui_overlay\"\n\n    def toggle_performance_monitor(self):\n        self.performance_monitor_visible = not self.performance_monitor_visible\n        self.last_action = \"toggle_performance_monitor\"\n\ndef handle_key_down_simulated(event_key: str, state: MockCombatScreenState):\n    \"\"\"\n    Simulates the handleKeyDown logic from CombatScreen3D.tsx.\n    This function takes a key string and a state object, then updates\n    the state based on the key pressed.\n    \"\"\"\n    if event_key == 'u':\n        state.toggle_ui_overlay()\n    elif event_key == 'p':\n        state.toggle_performance_monitor()\n    # Additional combat-specific shortcuts would be added here if they\n    # were part of the original TypeScript implementation.\n\n# --- Pytest Tests for the Simulated Logic ---\n\n@pytest.fixture\ndef combat_screen_state():\n    \"\"\"Provides a fresh mock state for each test function.\"\"\"\n    return MockCombatScreenState()\n\ndef test_handle_key_down_toggles_ui_overlay(combat_screen_state):\n    \"\"\"\n    Verifies that pressing the 'u' key correctly toggles the UI overlay visibility.\n    \"\"\"\n    # Initial state: UI overlay should be off\n    assert not combat_screen_state.ui_overlay_visible\n    assert combat_screen_state.last_action is None\n\n    # Simulate 'u' key press: UI overlay should turn on\n    handle_key_down_simulated('u', combat_screen_state)\n    assert combat_screen_state.ui_overlay_visible\n    assert combat_screen_state.last_action == \"toggle_ui_overlay\"\n\n    # Simulate 'u' key press again: UI overlay should turn off\n    handle_key_down_simulated('u', combat_screen_state)\n    assert not combat_screen_state.ui_overlay_visible\n    assert combat_screen_state.last_action == \"toggle_ui_overlay\"\n\ndef test_handle_key_down_toggles_performance_monitor(combat_screen_state):\n    \"\"\"\n    Verifies that pressing the 'p' key correctly toggles the performance monitor visibility.\n    \"\"\"\n    # Initial state: Performance monitor should be off\n    assert not combat_screen_state.performance_monitor_visible\n    assert combat_screen_state.last_action is None\n\n    # Simulate 'p' key press: Performance monitor should turn on\n    handle_key_down_simulated('p', combat_screen_state)\n    assert combat_screen_state.performance_monitor_visible\n    assert combat_screen_state.last_action == \"toggle_performance_monitor\"\n\n    # Simulate 'p' key press again: Performance monitor should turn off\n    handle