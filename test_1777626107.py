{
  "file_path": "tests/unit/combat/test_combat_input.py",
  "code": "import pytest\n\n# Mock GameState class to simulate game state and actions\nclass MockGameState:\n    def __init__(self):\n        self.is_combat_active = False\n        self.current_menu = None  # e.g., 'main_menu', 'combat_actions_menu'\n        self.player_health = 100\n        self.player_stamina = 50\n        self.player_actions = {\n            'attack': {'cost': 10, 'cooldown': 0},\n            'defend': {'cost': 5, 'cooldown': 0},\n            'special': {'cost': 30, 'cooldown': 5}\n        }\n        self.log_messages = []\n\n    def perform_action(self, action_name):\n        if action_name in self.player_actions:\n            action = self.player_actions[action_name]\n            if self.player_stamina >= action['cost']:\n                self.player_stamina -= action['cost']\n                self.log_messages.append(f\"Performed {action_name}. Stamina left: {self.player_stamina}\")\n                return True\n            else:\n                self.log_messages.append(f\"Not enough stamina for {action_name}.\")\n                return False\n        self.log_messages.append(f\"Unknown action: {action_name}\")\n        return False\n\n    def navigate_menu(self, direction):\n        if self.current_menu:\n            self.log_messages.append(f\"Navigating {self.current_menu} menu: {direction}\")\n            return True\n        self.log_messages.append(f\"No active menu to navigate.\")\n        return False\n\n    def select_menu_item(self):\n        if self.current_menu:\n            self.log_messages.append(f\"Selected item in {self.current_menu}.\")\n            return True\n        self.log_messages.append(f\"No active menu to select from.\")\n        return False\n\n    def close_menu(self):\n        if self.current_menu:\n            self.log_messages.append(f\"Closed {self.current_menu}.\")\n            self.current_menu = None\n            return True\n        self.log_messages.append(f\"No menu to close.\")\n        return False\n\n# Mock handleCombatInput function based on the summary\ndef handleCombatInput(event, game_state: MockGameState):\n    \"\"\"Orchestrates player combat actions and menu navigation by processing keyboard events based on current game state constraints.\"\"\"\n    key = event.get('key')\n    event_type = event.get('type', 'keydown') # Assume keydown by default\n\n    if event_type != 'keydown':\n        return # Only process keydown events for simplicity\n\n    if game_state.is_combat_active:\n        if game_state.current_menu == 'combat_actions_menu':\n            # Handle menu navigation within combat menu\n            if key == 'ArrowUp':\n                game_state.navigate_menu('up')\n            elif key == 'ArrowDown':\n                game_state.navigate_menu('down')\n            elif key == 'Enter':\n                game_state.select_menu_item()\n            elif key == 'Escape':\n                game_state.close_menu()\n            else:\n                game_state.log_messages.append(f\"Unhandled key in combat menu: {key}\")\n        else:\n            # Handle direct combat actions\n            if key == 'a': # Attack\n                game_state.perform_action('attack')\n            elif key == 'd': # Defend\n                game_state.perform_action('defend')\n            elif key == 's': # Special\n                game_state.perform_action('special')\n            elif key == 'Escape': # Open combat menu\n                game_state.current_menu = 'combat_actions_menu'\n                game_state.log_messages.append(\"Opened combat actions menu.\")\n            else:\n                game_state.log_messages.append(f\"Unhandled combat key: {key}\")\n    else: # Not in combat, assume general menu navigation or interaction\n        if game_state.current_menu:\n            if key == 'ArrowUp':\n                game_state.navigate_menu('up')\n            elif key == 'ArrowDown':\n                game_state.navigate_menu('down')\n            elif key == 'Enter':\n                game_state.select_menu_item()\n            elif key == 'Escape':\n                game_state.close_menu()\n            else:\n                game_state.log_messages.append(f\"Unhandled key in general menu: {key}\")\n        else:\n            # No menu open, general interaction (e.g., opening main menu)\n            if key == 'Escape':\n                game_state.current_menu = 'main_menu'\n                game_state.log_messages.append(\"Opened main menu.\")\n            else:\n                game_state.log_messages.append(f\"Unhandled non-combat, no-menu key: {key}\")\n\n\n@pytest.fixture\ndef game_state_fixture():\n    return MockGameState()\n\n# --- Test Scenarios for Combat Actions ---\n\ndef test_combat_attack_action(game_state_fixture):\n    game_state = game_state_fixture\n    game_state.is_combat_active = True\n    initial_stamina = game_state.player_stamina\n\n    handleCombatInput({'key': 'a'}, game_state)\n\n    assert game_state.player_stamina == initial_stamina - game_state.player_actions['attack']['cost']\n    assert \"Performed attack\" in game_state.log_messages[-1]\n\ndef test_combat_defend_action(game_state_fixture):\n    game_state = game_state_fixture\n    game_state.is_combat_active = True\n    initial_stamina = game_state.player_stamina\n\n    handleCombatInput({'key': 'd'}, game_state)\n\n    assert game_state.player_stamina == initial_stamina - game_state.player_actions['defend']['cost']\n    assert \"Performed defend\" in game_state.log_messages[-1]\n\ndef test_combat_special_action_insufficient_stamina(game_state_fixture):\n    game_state = game_state_fixture\n    game_state.is_combat_active = True\n    game_state.player_stamina = 10 # Set stamina too low for special (cost 30)\n    initial_stamina = game_state.player_stamina\n\n    handleCombatInput({'key': 's'}, game_state)\n\n    assert game_state.player_stamina == initial_stamina # Stamina should not change\n    assert \"Not enough stamina for special.\" in game_state.log_messages[-1]\n\ndef test_combat_action_when_not_active(game_state_fixture):\n    game_state = game_state_fixture\n    game_state.is_combat_active = False # Not in combat\n    initial_stamina = game_state.player_stamina\n\n    handleCombatInput({'key': 'a'}, game_state)\n\n    assert game_state.player_stamina == initial_stamina # Stamina should not change\n    assert \"Unhandled non-combat, no-menu key: a\" in game_state.log_messages[-1]\n\n# --- Test Scenarios for Menu Navigation ---\n\ndef test_open_combat_actions_menu(game_state_fixture):\n    game_state = game_state_fixture\n    game_state.is_combat_active = True\n    game_state.current_menu = None\n\n    handleCombatInput({'key': 'Escape'}, game_state)\n\n    assert game_state.current_menu == 'combat_actions_menu'\n    assert \"Opened combat actions menu.\" in game_state.log_messages[-1]\n\ndef test_navigate_combat_actions_menu(game_state_fixture):\n    game_state = game_state_fixture\n    game_state.is_combat_active = True\n    game_state.current_menu = 'combat_actions_menu'\n\n    handleCombatInput({'key': 'ArrowUp'}, game_state)\n    assert \"Navigating combat_actions_menu menu: up\" in game_state.log_messages[-1]\n\n    handleCombatInput({'key': 'ArrowDown'}, game_state)\n    assert \"Navigating combat_actions_menu menu: down\" in game_state.log_messages[-1]\n\ndef test_select_item_in_combat_actions_menu(game_state_fixture):\n    game_state = game_state_fixture\n    game_state.is_combat_active = True\n    game_state.current_menu = 'combat_actions_menu'\n\n    handleCombatInput({'key': 'Enter'}, game_state)\n\n    assert \"Selected item in combat_actions_menu.\" in game_state.log_messages[-1]\n\ndef test_close_combat_actions_menu(game_state_fixture):\n    game_state = game_state_fixture\n    game_state.is_combat_active =