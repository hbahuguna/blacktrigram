{
  "file_path": "tests/unit/systems/physics/test_collision_detection.py",
  "code": "import pytest\nfrom unittest.mock import patch, MagicMock\n\n# The target function 'int-disable-ne' is described as evaluating attack collisions.\n# Since the target code is unavailable, we will mock its behavior to test various scenarios\n# based on its summary: \"Evaluates whether an attack connects with a target by performing\n# multi-phase collision detection and calculating hit accuracy.\"\n# We assume a Python wrapper for this TypeScript function exists at\n# 'systems.physics.collision_detection.int_disable_ne'.\n\n@patch('systems.physics.collision_detection.int_disable_ne')\ndef test_int_disable_ne_basic_hit(mock_int_disable_ne: MagicMock):\n    \"\"\"Test case: A clear attack connects with high accuracy.\"\"\"\n    # Configure the mock to return a successful hit with high accuracy\n    mock_int