{
  "file_path": "tests/unit/systems/physics/test_collision_detection.py",
  "code": "import pytest\n\n# --- Target Function (Placeholder) ---\n# This is a Python implementation of the target TypeScript function\n# `calculateAttackReach` for testing purposes, as the original code was unavailable.\n# It's assumed the core logic is a multiplication of base range and a modifier,\n# with defensive checks for non-sensical negative inputs and invalid types.\n\ndef calculate_attack_reach(base_technique_range: float, stance_reach_modifier: float) -> float:\n    \"\"\"\n    Calculates the total effective reach of an attack by applying\n    stance-based modifiers to the base technique range.\n\n    Args:\n        base_technique_range: The base range of the attack technique.\n        stance_reach_modifier: The multiplier for reach from the current stance.\n\n    Returns:\n        The calculated effective reach. Returns 0.0 if inputs would result\n        in a negative reach.\n\n    Raises:\n        TypeError: If either input is not a numeric type (int or float).\n    \"