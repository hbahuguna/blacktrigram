{
  "file_path": "tests/unit/systems/physics/test_collision_detection.py",
  "code": "import pytest\n\n# Mock implementation of the target function, as the actual code is unavailable.\n# This mock simulates the behavior described in the symbol summary:\n# \"Evaluates whether an attack connects with a target by performing multi-phase\n# collision detection and calculating hit accuracy.\"\nclass MockCollisionDetection:\n    @staticmethod\n    def int_disable_ne(attacker_data: dict, target_data: dict, attack_data: dict) -> tuple[bool, float]:\n        """\n        Simulates multi-phase collision detection and hit accuracy calculation.\n\n        Phase 1: Basic Collision Detection (e.g., range check).\n        Phase 2: Accuracy Calculation (based on base accuracy, skill, dodge).\n        Phase 3: Hit Determination (simplified for deterministic testing).\n\n        Args:\n            attacker_data (dict): Contains 'position' (tuple), 'skill_modifier' (float).\n            target_data (dict): Contains 'position' (tuple), 'dodge_chance' (float).\n            attack_data (dict): Contains 'range' (float), 'base_accuracy' (float).\n\n        Returns:\n            tuple[bool, float]: (is_hit, final_accuracy)\n                is_hit (bool): True if the attack connects, False otherwise.\n                final_accuracy (float): The calculated hit accuracy (0.0 to 1.0).\n        """\n        attacker_pos = attacker_data.get('position', (0, 0, 0))\n        target_pos = target_data.get('position', (0, 0, 0))\n        attack_range = attack_data.get('range', 1.0)\n        base_accuracy = attack_data.get('base_accuracy', 0.75)\n        target_dodge = target_data.get('dodge_chance', 0.1)\n        attacker_skill = attacker_data.get('skill_modifier', 0.0)\n\n        # Phase 1: Collision Detection (simplified to a distance check)\n        distance = ((attacker_pos[0] - target_pos[0])**2 +\n                    (attacker_pos[1] - target_pos[1])**2 +\n                    (attacker_pos[2] - target_pos[2])**2)**0.5\n\n        is_colliding = distance <= attack_range\n\n        if not is_colliding:\n            return False, 0.0  # No collision, no hit, 0 accuracy\n\n        # Phase 2: Accuracy Calculation\n        # Combine base accuracy with attacker skill and target dodge\n        effective_accuracy = base_accuracy + attacker_skill - target_dodge\n\n        # Clamp accuracy between 0 and 1\n        effective_accuracy = max(0.0, min(1.0, effective_accuracy))\n\n        # Phase 3: Final Hit Determination\n        # For deterministic testing, we'll consider it a hit if accuracy is above a threshold.\n        # In a real game, this would involve a random roll against effective_accuracy.\n        is_hit = effective_accuracy > 0.5  # Arbitrary threshold for a 'hit'\n\n        return is_hit, effective_accuracy\n\n\n# Alias the mock function for easier testing\nint_disable_ne = MockCollisionDetection.int_disable_ne\n\n\ndef test_int_disable_ne_direct_hit():\n    """\n    Test a scenario where an attack should clearly hit with high accuracy.\n    """\n    attacker = {'position': (0, 0, 0), 'skill_modifier': 0.2}\n    target = {'position': (0.5, 0, 0), 'dodge_chance': 0.05}\n    attack = {'range': 1.0, 'base_accuracy': 0.8}\n\n    is_hit, accuracy = int_disable_ne(attacker, target, attack)\n\n    assert is_hit is True\n    # Expected accuracy: 0.8 (base) + 0.2 (skill) - 0.05 (dodge) = 0.95\n    assert accuracy == pytest.approx(0.95)\n\n\ndef test_int_disable_ne_miss_out_of_range():\n    """\n    Test a scenario where the target is out of the attack's range.\n    """\n    attacker = {'position': (0, 0, 0), 'skill_modifier': 0.1}\n    target = {'position': (2.0, 0, 0), 'dodge_chance': 0.1}\n    attack = {'range': 1.0, 'base_accuracy': 0.7}\n\n    is_hit, accuracy = int_disable_ne(attacker, target, attack)\n\n    assert is_hit is False\n    assert accuracy == pytest.approx(0.0) # No collision means 0 accuracy\n\n\ndef test_int_disable_ne_high_dodge_low_accuracy_miss():\n    """\n    Test a scenario where high dodge chance leads to a miss, even if in range.\n    """\n    attacker = {'position': (0, 0, 0), 'skill_modifier': -0.1}\n    target = {'position': (0.1, 0, 0), 'dodge_chance': 0.6}\n    attack = {'range': 1.0, 'base_accuracy': 0.7}\n\n    is_hit, accuracy = int_disable_ne(attacker, target, attack)\n\n    # Expected accuracy: 0.7 (base) - 0.1 (skill) - 0.6 (dodge) = 0.0\n    assert is_hit is False # Accuracy 0.0 is below 0.5 threshold\n    assert accuracy == pytest.approx(0.0)\n\n\ndef test_int_disable_ne_edge_case_just_in_range():\n    """\n    Test an attack that is exactly at the edge of its range.\n    """\n    attacker = {'position': (0, 0, 0), 'skill_modifier': 0.0}\n    target = {'position': (1.0, 0, 0), 'dodge_chance': 0.1}\n    attack = {'range': 1.0, 'base_accuracy': 0.7}\n\n    is_hit, accuracy = int_disable_ne(attacker, target, attack)\n\n    assert