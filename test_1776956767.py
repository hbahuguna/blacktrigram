```json
{
  "file_path": "tests/unit/systems/physics/test_collision_detection.py",
  "code": "import pytest\nfrom unittest.mock import MagicMock, patch\nimport math\nfrom dataclasses import dataclass\n\n# --- Data Structures (assumed from context) ---\n# In a real project, these would be imported from the application's model definitions.\n\n@dataclass\nclass Position:\n    x: float\n    y: float\n\n@dataclass\nclass Size:\n    width: float\n    height: float\n\n@dataclass\nclass Technique:\n    name: str\n    range: float\n\n# --- Target Function (recreated based on summary as code was unavailable) ---\n# This function would typically be imported, e.g.,\n# from src.systems.physics.CollisionDetection import checkAttackHit\n\ndef check_attack_hit(world, attacker_id, defender_id, technique):\n    \"\"\"\n    Evaluates whether a combat technique successfully connects with a defender.\n    Performs broad-phase distance checks and narrow-phase raycast validation.\n    \"\"\"\n    attacker_pos = world.get(attacker_id, \"position\")\n    attacker_size = world.get(attacker_id, \"size\")\n    defender_pos = world.get(defender_id, \"position\")\n    defender_size = world.get(defender_id, \"size\")\n\n    if not all([attacker_pos, attacker_size, defender_pos, defender_size]):\n        raise ValueError(\"Missing required components for collision check\")\n\n    # Broad-phase: distance check\n    distance = math.hypot(attacker_pos.x - defender_pos.x, attacker_pos.y - defender_pos.y)\n\n    # Calculate combined size for a more generous broad-phase check\n    max_attacker_dim = max(attacker_size.width, attacker_size.height)\n    max_defender_dim = max(defender_size.width, defender_size.height)\n    \n    # The maximum distance for a potential hit (attack range + half of each entity's max dimension)\n    max_reach = technique.range + (max_attacker_dim / 2) + (max_defender_dim / 2)\n\n    if distance > max_reach:\n        return False  # Too far away, fail broad-phase\n\n    # If entities are overlapping, it's a guaranteed hit.\n    if distance == 0:\n        return True\n\n    # Narrow-phase: raycast validation\n    # In a real implementation, a raycast function would be called here.\n    # We mock this dependency for the unit test.\n    hit_result = world.raycast(attacker_id, defender_id, technique.range)\n    \n    return hit_result\n\n# --- Test Setup ---\n\n@pytest.fixture\ndef mock_world():\n    \"\"\"Creates a mock world object with a configurable 'get' method.\"\"\"\n    world = MagicMock()\n    entities = {}\n\n    def get_component(entity_id, component_name):\n        return entities.get(entity_id, {}).get(component_name)\n\n    def add_entity(entity_id, components):\n        entities[entity_id] = components\n\n    world.get = MagicMock(side_effect=get_component)\n    # Helper to set up the mock entities within tests\n    world.add_entity = add_entity\n    # Mock the raycast dependency\n    world.raycast = MagicMock()\n    return world\n\n# --- Test Cases ---\n\n@pytest.mark.parametrize(\n    \"attacker_pos, defender_pos, technique_range, raycast_succeeds, expected_hit, description\",\n    [\n        (Position(0, 0), Position(5, 0), 6.0, True, True, \"Clear hit within range\"),\n        (Position(0, 0), Position(10, 0), 8.0, True, False, \"Miss due to being out of range (broad-phase fail)\"),\n        (Position(0, 0), Position(5, 0), 6.0, False, False, \"Miss due to raycast failure (narrow-phase fail)\"),\n        (Position(0, 0), Position(0, 0), 1.0, True, True, \"Guaranteed hit when entities are overlapping\"),\n        (Position(0, 0), Position(8.0, 0), 8.0, True, True, \"Hit at the exact maximum edge of the technique range\"),\n        (Position(0, 0), Position(8.1, 0), 8.0, True, False, \"Miss just outside the maximum technique range\"),\n        (Position(10, 10), Position(13, 14), 5.0, True, True, \"Hit with diagonal positioning\"),\n        (